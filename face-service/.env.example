# Face Recognition Service Configuration

# Service settings
SERVICE_HOST=0.0.0.0
SERVICE_PORT=5001

# Model settings
# Options: buffalo_l (most accurate), buffalo_s (faster), buffalo_sc (smallest)
INSIGHTFACE_MODEL=buffalo_l

# Detection settings
DETECTION_SIZE=640

# Matching threshold (cosine similarity)
# Adjust based on your testing: 0.4-0.5 typical for InsightFace
FACE_MATCH_THRESHOLD=0.45

# Execution provider
# Options: CPUExecutionProvider, CUDAExecutionProvider (if GPU available)
EXECUTION_PROVIDER=CPUExecutionProvider
